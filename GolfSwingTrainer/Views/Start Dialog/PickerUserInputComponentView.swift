//
//  PickerUserInputComponentView.swift
//  GolfSwingTrainer
//
//  Created by David on 2024-11-12.
//

import SwiftUI

/*
struct PickerUserInputComponentView: View {
    let title: String
       let items: [String]
       @Binding var selection: String
       var pickerStyle: PickerStyleType
    var body: some View {
        HStack {
            Image(systemName: "list.bullet")
            Text(title)
            
            Picker(title, selection: $selection) {
                ForEach(items, id: \.self) { item in
                    Text(item)
                }
            }
            .pickerStyle(getPickerStyle(for: pickerStyle))
        }
        .font(.headline).fontWeight(.semibold)
    }
    
}


#Preview {
    PickerUserInputComponentView()
}
*/
